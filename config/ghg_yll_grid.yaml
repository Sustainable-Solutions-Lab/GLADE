# SPDX-FileCopyrightText: 2026 Koen van Greevenbroek
#
# SPDX-License-Identifier: CC-BY-4.0

# GHG/YLL Grid Sensitivity Analysis Configuration
# -----------------------------------------------
# Mirrors the combined GHG+YLL setup from sensitivity.yaml, but explores
# the full Cartesian grid of GHG price and YLL value combinations.

name: "ghg_yll_grid"
scenarios:
  # GHG/YLL Grid Sensitivity Scenarios
  # ----------------------------------
  # Full 2D grid over GHG prices and YLL values.

  # Baseline scenario for calibrating piecewise food utility blocks (dual variables)
  baseline:
    validation:
      enforce_baseline_diet: true
    food_utility_piecewise:
      enabled: false
    emissions:
      ghg_price: 0
    health:
      value_per_yll: 0

  ghg_yll_0:
    emissions:
      ghg_price: 0
    health:
      value_per_yll: 0
    macronutrients:
      cal:
        equal_to_baseline: true

  # =============================================================================
  # Generated Scenarios (full grid)
  # =============================================================================

  _generators:
    - name: ghg_yll_{ghg}_{yll}
      mode: grid
      parameters:
        ghg:
          space: log
          start: 5
          stop: 500
          num: 10
          round: true
        yll:
          space: log
          start: 50
          stop: 50000
          num: 10
          round: true
      template:
        emissions:
          ghg_price: "{ghg}"
        health:
          value_per_yll: "{yll}"
        macronutrients:
          cal:
            equal_to_baseline: true

# Set planning horizon and baseline year to match GDD baseline data (2018)
planning_horizon: 2020
baseline_year: 2020

# Block-based diminishing marginal utility for food consumption
food_utility_piecewise:
  enabled: true

validation:
  use_actual_yields: true
  production_stability:
    enabled: true
    penalty_mode: "l1"
    deviation_type: "absolute"

# Compare all scenarios in plots
plotting:
  comparison_scenarios: "all"

solving:
  solver: "gurobi"
  io_api: "direct"
  threads: 2
  mem_mb: 9000
  runtime: 60
